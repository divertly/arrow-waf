core:
  test_mode: false
  signal_interval: 100

waf:
  host: 0.0.0.0
  port: 6080

log:
  format: text
  level: warn
  color: false

system:
  host: 0.0.0.0
  port: 6081

testing:
  profile_cpu: false
  profile_ram: false

upstream:
  host: localhost
  port: 8000
  protocol: http
